/*package com.gestao_restaurante.repository;

import com.gestao_restaurante.model.Mesa;

import java.util.ArrayList;

 */

/*
public class MesaRepository {
    private ArrayList<Mesa> mesas;

    public MesaRepository(){
        mesas = new ArrayList<>();
    }

    public void criarMesa(int numeroMesa){
        mesas.get(index) = new Mesa(numeroMesa);
    }

    public Mesa selecionarMesa(int numeroMesa){
        return mesas.get(numeroMesa);
    }

    public void visualizarMesas(){
        for(Mesa m: mesas){
            if(m == null)
                break;
            System.out.println("Mesa" + "-" + m.getNumero());
        }
    }
}
 */
